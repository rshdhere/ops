apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: devin-brain
  namespace: devin-app
spec:
  dataFrom:
    - extract:
        key: staging/brain
        conversionStrategy: Default
        decodingStrategy: None
        metadataPolicy: None
