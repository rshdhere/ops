# Staging copy of orchestrator RBAC.
# The ServiceAccount is namespaced (lives in devin-staging) so it keeps its
# name, but the ClusterRole/ClusterRoleBinding are cluster-scoped and must use
# staging-unique names to avoid colliding with the production devin app.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: devin-orchestrator
  namespace: devin-system
  annotations:
    argocd.argoproj.io/sync-wave: "1"
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: staging-devin-orchestrator
  annotations:
    argocd.argoproj.io/sync-wave: "1"
rules:
  - apiGroups: [""]
    resources: ["nodes"]
    verbs: ["get", "list", "watch"]
  - apiGroups: ["devin"]
    resources:
      - sandboxes
      - sandboxes/status
      - firecrackermachines
      - firecrackermachines/status
      - firecrackerhosts
      - firecrackerhosts/status
      - snapshots
      - snapshots/status
    verbs: ["get", "list", "watch", "create", "update", "patch", "delete"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: staging-devin-orchestrator
  annotations:
    argocd.argoproj.io/sync-wave: "1"
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: staging-devin-orchestrator
subjects:
  - kind: ServiceAccount
    name: devin-orchestrator
    namespace: devin-system
