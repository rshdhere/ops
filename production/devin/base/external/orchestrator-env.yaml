apiVersion: apps/v1
kind: Deployment
metadata:
  name: devin-orchestrator
  namespace: devin-system
spec:
  template:
    spec:
      containers:
        - name: orchestrator
          env:
            - name: ORCHESTRATOR_NODE_REGISTER_ENABLED
              value: "false"
